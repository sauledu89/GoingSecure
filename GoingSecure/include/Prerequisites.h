#pragma once

/// @brief Entrada y salida estándar en consola (std::cin, std::cout).
#include <iostream>

/// @brief Manipulación de cadenas a través de flujos de texto (std::ostringstream, std::istringstream).
#include <sstream>

/// @brief Estructura de datos dinámica (std::vector).
/// @note Muy útil en videojuegos para manejar listas de objetos como enemigos, partículas, inventarios, etc.
#include <vector>

/// @brief Funciones y clases para manejo de cadenas de caracteres (std::string).
#include <string>

/// @brief Formato de salida manipulable (std::setw, std::setfill, std::hex, etc.).
/// @note Comúnmente usado para visualizar datos cifrados o depurar valores numéricos en consola.
#include <iomanip>

/// @brief Funciones de clasificación de caracteres (std::isdigit, std::isalpha, std::isspace, etc.).
#include <cctype>

/// @brief Algoritmos generales de STL (std::sort, std::find, std::all_of, etc.).
/// @note Relevantes para implementar lógica de juego eficiente o validaciones.
#include <algorithm>

/// @brief Representación de datos en binario fijo (std::bitset).
/// @note Útil para operaciones a bajo nivel como manipulación de bits, flags o codificación binaria.
#include <bitset>
